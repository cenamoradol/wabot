[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run prisma:generate && npm run build && npx prisma migrate deploy --schema=./prisma/schema.prisma"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health/live"
healthcheckTimeout = 30